# Logging (1=debug, 2=info, 3=warning, 4=error)
log_level=2

# Paths
log_path=./logs/castor.log
socket_path=./socket/castor.socket
audio_source_path=./audio/source
audio_playlist_path=./audio/playlist
# audio_record_path=./audio/recordings
audio_fallback_path=./audio/fallback

# Caching time (sec.)
preload_time_file=3600
preload_time_fallback=3600

# Fallback Track Shuffling (random ordering each reload; 0 = off)
fallback_shuffle=1

# Fallback Test Signal (synthesized sine waves; 0 = off)
fallback_sine_synth=1

# Silence Detector
silence_threshold=-90
silence_start_duration=10
silence_stop_duration=1

# Fading
program_fade_in_time=1.0
program_fade_out_time=1.0
fallback_cross_fade_time=5.0

# Audio Device
in_device_name=Soundcraft Signature 12 MTK: USB Audio
out_device_name=Soundcraft Signature 12 MTK: USB Audio
sample_rate=44100

# Stream Output (comment-out stream_out_url to disable)
# stream_out_url=icecast://source:hackme@localhost:8000/radio.mp3
stream_out_metadata_url=http://source:hackme@localhost:8000/admin/metadata?mount=/radio.mp3
stream_out_name=Radio Station
stream_out_description=Live Stream
stream_out_genre=Electronic
stream_out_href=http://localhost:8000/radio.mp3

# API (comment-out to disable)
# program_url=http://aura.local/steering/api/v1/program/playout/
# media_url=http://aura.local/steering/api/v1/media/
# playlog_url=http://localhost:8010/api/v1/playlog
# health_url=http://localhost:8010/api/v1/source/health/1
# clock_url=http://localhost:8010/api/v1/clock
calendar_refresh_interval=60
health_report_interval=10

# TCP Server
tcp_port=1234